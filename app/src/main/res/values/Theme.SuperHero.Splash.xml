<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema del splash -->
    <style name="Theme.SuperHero.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Este icono es opcional -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item> -->
        <item name="postSplashScreenTheme">@style/Theme.SuperHero</item>
    </style>

    <!-- Tema principal de la app -->
    <style name="Theme.SuperHero" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/splash_background</item>
        <item name="colorOnPrimary">@color/teal_200</item>
    </style>

</resources>



